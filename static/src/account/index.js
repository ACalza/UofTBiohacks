import React, { Component } from 'react'
import mount from '../mount.js'

class Account extends Component {
  render() {
    return(
      <h2>Account Page</h2>
    )
  }
}

export default mount(null, null)
